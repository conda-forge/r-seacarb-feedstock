{% set version = "3.2.11" %}
{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-seacarb
  version: {{ version|replace("-", "_") }}

source:
  fn: seacarb_{{ version }}.tar.gz
  url:
    - {{ cran_mirror }}/src/contrib/seacarb_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/seacarb/seacarb_{{ version }}.tar.gz
  sha256: e6b3e7efc927a03858b3c8703b736e4be25b384afba31ec517dbbb9e2edaa929

build:
  noarch: generic
  merge_build_host: True  # [win]
  number: 0
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{posix}}zip               # [win]
  host:
    - r-base
    - r-gsw
    - r-oce
  run:
    - r-base
    - r-gsw
    - r-oce

test:
  commands:
    - $R -e "library('seacarb')"           # [not win]
    - "\"%R%\" -e \"library('seacarb')\""  # [win]

about:
  home: https://CRAN.R-project.org/package=seacarb
  license: GPL (>= 2)
  summary: Calculates parameters of the seawater carbonate system and assists the design of ocean
    acidification perturbation experiments.
  license_family: GPL3
  license_file: '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - johanneskoester
    - bgruening
    - daler
    - jdblischak
    - cbrueffer
    - dbast
